// starting pokemons
const usersPokemon = [
  "https://pokeapi.co/api/v2/pokemon/bulbasaur",
  "https://pokeapi.co/api/v2/pokemon/charizard",
  "https://pokeapi.co/api/v2/pokemon/poliwhirl"
]


const UserPokemons = (pokemon, index) => {
  
/*   useEffect(() => {
    async function ownPokeFetch (){
      usersPokemon.map((poke))
      const response = await fetch({poke});
      const ownPoke = await response.json();
      return ownPoke
    }
  }, [])
   */
  return ( 
    <div></div>
    /* ownPokeFetch(ownPoke)
    <div>{ownPoke.name}</div>
    )
    
     */
    );
    
  }
 
export default UserPokemons;